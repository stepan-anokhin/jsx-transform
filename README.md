# Transform JSX to TSX

Utility to transform JSX files to TSX.

## Usage

Compile sources:

```shell
npm install
npx tsc
```

Transform JSX files:

```shell
node src/commands/convert.js path/to/components/**/*.jsx
```
